<?xml version="1.0" encoding="utf-8"?>
<!--
  Settings Menu Row Item
  Redesigned using:
    - Emil Kowalski: stateListAnimator scale 0.97 on press — tactile feedback
    - impeccable: icon container gives visual grouping; contrast-compliant subtitle #a8a8a8 (vs banned #888888)
    - taste-skill: one consistent icon container size (40dp), no random icon-in-line
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="64dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:stateListAnimator="@animator/press_scale"
    android:paddingStart="@dimen/spacing_lg"
    android:paddingEnd="@dimen/spacing_lg"
    android:paddingTop="@dimen/spacing_xs"
    android:paddingBottom="@dimen/spacing_xs">

    <!-- Icon container: 40dp circle with brand-tinted background -->
    <FrameLayout
        android:id="@+id/icon_container"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_settings_icon_circle">

        <ImageView
            android:id="@+id/menuIcon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_settings"
            app:tint="?attr/colorPrimary" />
    </FrameLayout>

    <!-- Title + summary column -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="@dimen/spacing_md"
        android:orientation="vertical">

        <TextView
            android:id="@+id/menuText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Settings Item"
            android:textColor="?attr/colorOnBackground"
            android:letterSpacing="0.01"
            android:textAppearance="@style/TextAppearance.App.TitleMedium" />

        <TextView
            android:id="@+id/menuSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Summary text"
            android:textColor="?attr/appColorMutedForeground"
            android:textAppearance="@style/TextAppearance.App.BodySmall"
            android:maxLines="2"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Chevron — slightly larger for readability -->
    <ImageView
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/ic_action_forward"
        app:tint="?attr/appColorSubtleForeground" />

</LinearLayout>
