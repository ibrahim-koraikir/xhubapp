<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tab_item_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_tab_grid_card"
    android:elevation="2dp"
    android:orientation="vertical"
    android:padding="0dp">

    <!-- Card Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@drawable/bg_tab_grid_card_header_glass">

        <fulguris.view.ImageView
            android:id="@+id/faviconTab"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="8dp"
            app:tint="@color/tab_grid_foreground"
            tools:src="@drawable/ic_web" />

        <TextView
            android:id="@+id/textTab"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/tab_grid_card_foreground"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="IMDb: Ratings, Reviews, and Where to Watch" />

        <!-- Close button -->
        <LinearLayout
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:background="@drawable/bg_tab_grid_close_btn"
            android:gravity="center">

            <fulguris.view.ImageView
                android:id="@+id/deleteAction"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_action_delete"
                app:tint="@color/tab_grid_muted_foreground" />
        </LinearLayout>

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/tab_grid_secondary"/>

    <!-- Card Preview -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:background="@color/tab_grid_background">

        <ImageView
            android:id="@+id/tabPreviewImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_explore_outline" /> <!-- We use centerCrop like object-fit: cover -->

    </FrameLayout>

</LinearLayout>
