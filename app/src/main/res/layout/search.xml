<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@null"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:elevation="0dp"
        android:descendantFocusability="beforeDescendants"
        android:focusableInTouchMode="true"
        android:focusable="true"
        android:paddingStart="4dp"
        android:paddingEnd="0dp">

        <com.xhub.browser.view.ImageView
            android:id="@+id/search_ssl_status"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@color/transparent"
            android:padding="6dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:focusable="false"
            android:visibility="gone"
            />

        <!-- Ask Input -->
        <com.xhub.browser.view.SearchView
            android:id="@+id/search"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:background="@null"
            android:completionThreshold="1"
            android:dropDownAnchor="@id/toolbar_include"
            android:dropDownWidth="match_parent"
            android:gravity="start|center_vertical"
            android:hint="@string/search_overlay_hint"
            android:imeOptions="actionGo"
            android:importantForAutofill="no"
            android:inputType="textUri"
            android:selectAllOnFocus="true"
            android:textColor="?attr/colorOnBackground"
            android:textColorHint="?attr/appColorSubtleForeground"
            android:textCursorDrawable="@null"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            app:layout_constraintEnd_toStartOf="@+id/ask_actions"
            app:layout_constraintStart_toEndOf="@+id/search_ssl_status"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="0dp"
            android:ellipsize="middle"
            android:scrollHorizontally="true"
            android:singleLine="true"
            android:focusableInTouchMode="true"
            android:clickable="true"
            android:focusable="true"
            />

        <!-- Ask Actions (Hidden as we handle these natively in toolbar) -->
        <LinearLayout
            android:id="@+id/ask_actions"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:visibility="gone"
            android:gravity="center_vertical"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Toolbar Chip (Plus Icon) -->
            <LinearLayout
                android:id="@+id/attach_chip"
                android:visibility="gone"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:background="@drawable/toolbar_chip_bg"
                android:gravity="center"
                android:paddingHorizontal="10dp"
                android:layout_marginEnd="6dp"
                android:clickable="true"
                android:focusable="true">
                
                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@drawable/ic_action_plus"
                    app:tint="?attr/appColorMutedForeground" />
            </LinearLayout>

            <!-- Mic Button -->
            <FrameLayout
                android:id="@+id/mic_button"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/control_circle_bg"
                android:layout_marginEnd="6dp"
                android:clickable="true"
                android:focusable="true">
                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_mic"
                    app:tint="?attr/appColorMutedForeground" />
            </FrameLayout>

            <!-- Send Button -->
            <FrameLayout
                android:id="@+id/send_button"
                android:visibility="gone"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/control_circle_primary_bg"
                android:clickable="true"
                android:focusable="true">
                <ImageView
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_action_forward"
                    app:tint="?attr/colorSurface" />
            </FrameLayout>
            
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>