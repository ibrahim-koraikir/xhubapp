<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone"
    tools:visibility="visible">

    <!-- Full-screen Starfield Background Image -->
    <ImageView
        android:id="@+id/homeScreenBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_home_starfield"
        android:contentDescription="@null"
        android:importantForAccessibility="no" />

    <!-- 40% Transparent Dark Overlay for Text/Icon Contrast -->
    <View
        android:id="@+id/homeScreenBackgroundOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#66000000"
        android:importantForAccessibility="no" />

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/homeAppBarLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/home_appbar_height"
        android:background="@android:color/transparent"
        app:elevation="0dp">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/homeCollapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:contentScrim="@color/home_background"
            app:titleEnabled="false">

            <!-- Expanded Header: Brand + Greeting -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center"
                android:paddingTop="16dp"
                android:paddingBottom="8dp"
                app:layout_collapseMode="parallax">

                <!-- Logo row -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_gravity="center_horizontal">

                    <FrameLayout
                        android:layout_width="@dimen/home_logo_badge_size"
                        android:layout_height="@dimen/home_logo_badge_size"
                        android:background="@drawable/bg_home_logo_gradient"
                        android:layout_marginEnd="14dp"
                        android:padding="1dp">

                        <ImageView
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:layout_gravity="center"
                            android:src="@drawable/ic_comet_logo"
                            android:contentDescription="@null"
                            android:importantForAccessibility="no"
                            android:scaleType="fitCenter"
                            android:padding="8dp"
                            app:tint="@color/home_foreground" />
                    </FrameLayout>

                    <!-- Brand name — only in expanded header -->
                    <TextView
                        android:id="@+id/homeTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/home_brand_name"
                        android:textColor="@color/home_foreground"
                        android:textSize="@dimen/home_header_title_size"
                        android:fontFamily="sans-serif-black"
                        android:includeFontPadding="false"
                        android:letterSpacing="0.01" />
                </LinearLayout>

                <!-- Greeting sub-line: updated in code; placeholder keeps layout preview readable -->
                <TextView
                    android:id="@+id/homeGreeting"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/home_greeting_morning"
                    android:textColor="@color/home_muted_foreground"
                    android:textSize="@dimen/home_header_subtitle_size"
                    android:fontFamily="sans-serif-light"
                    android:letterSpacing="0.04"
                    android:layout_marginTop="6dp"
                    android:layout_gravity="center_horizontal" />

            </LinearLayout>

            <!-- Collapsed Toolbar (pinned) — brand name omitted here; expanded header carries it -->
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/homeToolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin"
                app:contentInsetStart="0dp"
                app:contentInsetStartWithNavigation="0dp">

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:paddingHorizontal="16dp">

                    <!-- Settings button (left) -->
                    <FrameLayout
                        android:id="@+id/homeSettingsBtnContainer"
                        android:layout_width="@dimen/home_avatar_size"
                        android:layout_height="@dimen/home_avatar_size"
                        android:background="@drawable/bg_home_profile_ring"
                        android:padding="1dp"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent">

                        <ImageButton
                            android:id="@+id/homeSettingsBtn"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:src="@drawable/ic_settings"
                            android:padding="10dp"
                            android:scaleType="fitCenter"
                            app:tint="@color/home_foreground"
                            android:contentDescription="@string/home_settings_desc" />
                    </FrameLayout>

                    <!-- Collapsed brand name — centered between the two icon buttons -->
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/home_brand_name"
                        android:textColor="@color/home_foreground"
                        android:textSize="@dimen/home_collapsed_title_size"
                        android:fontFamily="sans-serif-black"
                        app:layout_constraintLeft_toRightOf="@id/homeSettingsBtnContainer"
                        app:layout_constraintRight_toLeftOf="@id/homeBookmarksButton"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent" />

                    <!-- Right button: Tinder-style flame (bookmarks) -->
                    <FrameLayout
                        android:id="@+id/homeBookmarksButton"
                        android:layout_width="@dimen/home_avatar_size"
                        android:layout_height="@dimen/home_avatar_size"
                        android:background="@drawable/bg_home_profile_ring"
                        android:padding="1dp"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent">

                        <com.google.android.material.imageview.ShapeableImageView
                            android:id="@+id/homeBookmarksIcon"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:scaleType="fitCenter"
                            android:padding="10dp"
                            android:contentDescription="@string/home_bookmarks_desc"
                            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"
                            android:src="@drawable/ic_launcher_foreground" />
                    </FrameLayout>

                </androidx.constraintlayout.widget.ConstraintLayout>

            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Scrollable body -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:scrollbars="none"
        android:overScrollMode="never"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/home_padding_horizontal"
            android:paddingTop="@dimen/home_padding_top"
            android:paddingBottom="@dimen/home_padding_bottom">

            <!-- ── SHORTCUTS SECTION ────────────────────────────────────── -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/bg_home_shortcut_card"
                android:padding="@dimen/home_card_padding">

                <!-- Section header row -->
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="@dimen/home_section_header_min_height"
                    android:layout_marginBottom="@dimen/home_section_header_margin_bottom">

                    <TextView
                        android:id="@+id/shortcutsTitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:text="@string/home_quick_access_title"
                        android:textColor="@color/home_foreground"
                        android:textSize="@dimen/home_section_title_size"
                        android:fontFamily="sans-serif-medium"
                        android:letterSpacing="0.01"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toStartOf="@+id/btnEditShortcuts"
                        app:layout_constraintBottom_toTopOf="@+id/shortcutsSubtitle" />

                    <TextView
                        android:id="@+id/shortcutsSubtitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:text="@string/home_quick_access_subtitle"
                        android:textColor="@color/home_subtle_foreground"
                        android:textSize="@dimen/home_section_subtitle_size"
                        android:fontFamily="sans-serif"
                        app:layout_constraintTop_toBottomOf="@+id/shortcutsTitle"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintEnd_toStartOf="@+id/btnEditShortcuts"
                        app:layout_constraintBottom_toBottomOf="parent" />

                    <!-- Edit button with ripple feedback -->
                    <LinearLayout
                        android:id="@+id/btnEditShortcuts"
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/home_edit_btn_height"
                        android:orientation="horizontal"
                        android:gravity="center"
                        android:paddingHorizontal="@dimen/home_edit_btn_padding_h"
                        android:background="@drawable/bg_shortcut_manager_icon_btn"
                        android:foreground="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true"
                        android:contentDescription="@string/home_edit_shortcuts_desc"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent">

                        <ImageView
                            android:layout_width="@dimen/home_edit_icon_size"
                            android:layout_height="@dimen/home_edit_icon_size"
                            android:src="@drawable/ic_edit_outline"
                            android:contentDescription="@null"
                            android:importantForAccessibility="no"
                            android:tint="@color/home_dim_foreground" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="@dimen/home_edit_icon_label_gap"
                            android:text="@string/home_edit_shortcuts"
                            android:textColor="@color/home_dim_foreground"
                            android:textSize="@dimen/home_edit_text_size"
                            android:fontFamily="sans-serif-medium" />

                    </LinearLayout>

                </androidx.constraintlayout.widget.ConstraintLayout>

                <!-- Dynamic shortcut tile grid populated in code -->
                <LinearLayout
                    android:id="@+id/shortcutsDynamicContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
