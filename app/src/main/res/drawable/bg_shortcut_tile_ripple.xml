<?xml version="1.0" encoding="utf-8"?>
<!-- Circular ripple for shortcut tiles — dark ripple on the tile surface -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/home_icon_tint">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/home_tile_surface" />
            <stroke android:width="@dimen/home_tile_stroke_width" android:color="@color/home_stroke" />
        </shape>
    </item>
</ripple>
