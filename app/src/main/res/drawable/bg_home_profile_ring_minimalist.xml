<?xml version="1.0" encoding="utf-8"?>
<!-- Premium profile ring: refined circular container with subtle accent ring -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Subtle vivid magenta accent ring -->
    <stroke 
        android:width="2dp" 
        android:color="@color/home_accent_ring" />
    <!-- Faint accent-tinted fill for premium depth -->
    <solid android:color="@color/home_accent_ring_15" />
</shape>
