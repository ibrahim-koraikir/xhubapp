<?xml version="1.0" encoding="utf-8"?>
<!-- Short accent underline beneath shortcut group titles (SOCIAL, NEWS, …).
     Mirrors bg_home_quote_divider so the hero + group header share one visual language. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:startColor="?attr/appColorAccentOrangeFaint"
        android:centerColor="?attr/appColorAccentOrange"
        android:endColor="?attr/appColorAccentOrangeFaint"
        android:angle="0" />
    <size android:width="@dimen/home_group_underline_width" android:height="@dimen/home_group_underline_height" />
</shape>
