<?xml version="1.0" encoding="utf-8"?>
<!-- Static squircle background for shortcut tiles (replaces the MaterialCardView look).
     Drawn as the FrameLayout background so children (letter initial + favicon) render on top.
     Background color is swapped to white at runtime when a favicon loads. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/home_tile_surface" />
    <stroke
        android:width="@dimen/home_tile_stroke_width"
        android:color="@color/home_tile_stroke" />
    <corners android:radius="@dimen/home_tile_radius" />
</shape>
