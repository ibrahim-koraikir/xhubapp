<?xml version="1.0" encoding="utf-8"?>
<!-- Premium logo badge: refined circular badge with subtle accent ring -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Dark surface fill -->
    <solid android:color="@color/home_secondary" />
    <!-- Subtle vivid magenta accent ring -->
    <stroke 
        android:width="2dp" 
        android:color="@color/home_accent_ring" />
</shape>
