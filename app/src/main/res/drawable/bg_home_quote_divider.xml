<?xml version="1.0" encoding="utf-8"?>
<!-- Horizontal divider line above the "Quote of the day" label.
     Transparent → orange (~50% alpha) → transparent.
     NOTE: gradient endpoints use solid appColorAccentOrange; the 0-alpha endpoints
     are kept hardcoded (#00FF6B35 form) since alpha-0 reads identically on any theme. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:startColor="?attr/appColorAccentOrangeFaint"
        android:centerColor="?attr/appColorAccentOrange"
        android:endColor="?attr/appColorAccentOrangeFaint"
        android:angle="0" />
    <size android:width="@dimen/spacing_xl" android:height="@dimen/elevation_sm" />
</shape>
